<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".ImpulseApplication"
        android:allowBackup="false"
        android:appCategory="game"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.Impulse"
        android:usesCleartextTraffic="false">
        <!--
            Hochkant ist für die gesamte App gewollt; nur das Funfacts-Malen gibt die Drehung zur Laufzeit frei.
            Deshalb werden ausschließlich die Orientierungswarnungen hier unterdrückt. Als appCategory="game"
            fällt Impulse unter die Spiele-Ausnahme der Orientierungsänderungen ab Android 16.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            tools:ignore="LockedOrientationActivity,DiscouragedApi">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
